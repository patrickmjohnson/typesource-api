{
  "version": 2,
  "builds": [
    { "src": "api/*.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" }
  ],
  "env": {
    "ALLOWED_ORIGINS": "@typesource-allowed-origins",
    "OPENAI_API_KEY": "@typesource-openai-key"
  }
} 